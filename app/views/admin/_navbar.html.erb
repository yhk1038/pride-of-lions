<div class="well well-out">
    <div class="well well-in">
        <table class="table" style="border-color: black;">
            <thead>
                <tr class="tr-head">
                    <td>#</td>
                    <td>제목</td>
                    <td>순서</td>
                </tr>
            </thead>
            <tbody>
                <% Navbar.all.reverse.each do |post| %>
                    <tr class="tr-content">
                        <td><%= post.id %></td>
                        <td><a href="/admin/index?admin=m_nav&id=<%= post.id %>&mod=1"><%= post.title %></a></td>
                        <td><%= post.index %></td>
                    </tr>
                <% end %>
        </table>
    </div>

    <div class="row" align="right">
        
        <a class="btn btn-orange" href="/admin/index?admin=m_nav&mod=0">등록</a>
    
    </div>


</div>
